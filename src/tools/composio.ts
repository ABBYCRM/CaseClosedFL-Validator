import { env } from "../config/env.js";
export type Capability="WEB_SEARCH"|"WEB_EXTRACT"|"JS_BROWSER"|"PUBLIC_RECORD_LOOKUP"|"BUSINESS_SEARCH"|"COURT_SEARCH"|"PROVIDER_SEARCH";export interface ComposioTool{slug:string;name?:string;description?:string;input_schema?:unknown;toolkit?:string;}export interface ComposioSession{id:string;}
function headers(){return {"x-api-key":env.COMPOSIO_API_KEY,"Content-Type":"application/json"};}async function call(path:string,init:RequestInit={}){if(!env.COMPOSIO_API_KEY)throw new Error("COMPOSIO_NOT_CONFIGURED");const c=new AbortController();const timer=setTimeout(()=>c.abort(),env.TOOL_TIMEOUT_MS);try{const r=await fetch(`${env.COMPOSIO_BASE_URL.replace(/\/$/,"")}${path}`,{...init,signal:c.signal,headers:{...headers(),...(init.headers??{})}});const text=await r.text();if(!r.ok)throw new Error(`COMPOSIO_${r.status}:${text.slice(0,400)}`);return text?JSON.parse(text):{};}finally{clearTimeout(timer);}}
export async function createSession():Promise<ComposioSession>{const payload:any={user_id:env.COMPOSIO_USER_ID};if(env.COMPOSIO_TOOLKITS.length)payload.toolkits={enabled:env.COMPOSIO_TOOLKITS};const j:any=await call("/api/v3.1/tool_router/session",{method:"POST",body:JSON.stringify(payload)});const id=j.id??j.session_id??j.data?.id??j.data?.session_id;if(!id)throw new Error("COMPOSIO_SESSION_ID_MISSING");return{id};}
export async function searchTools(sessionId:string,query:string):Promise<ComposioTool[]>{const j:any=await call(`/api/v3.1/tool_router/session/${encodeURIComponent(sessionId)}/search`,{method:"POST",body:JSON.stringify({query})});const candidates=j.tools??j.items??j.data?.tools??j.data?.items??j.data??[];return Array.isArray(candidates)?candidates.map((x:any)=>({slug:x.slug??x.tool_slug??x.name,name:x.name,description:x.description,input_schema:x.input_schema??x.inputSchema,toolkit:x.toolkit??x.toolkit_slug})).filter((x:any)=>x.slug):[];}
export async function executeTool(sessionId:string,slug:string,args:Record<string,unknown>){return call(`/api/v3.1/tool_router/session/${encodeURIComponent(sessionId)}/execute`,{method:"POST",body:JSON.stringify({tool_slug:slug,arguments:args})});}
export async function executeMeta(sessionId:string,slug:"COMPOSIO_SEARCH_TOOLS"|"COMPOSIO_GET_TOOL_SCHEMAS",args:Record<string,unknown>){return call(`/api/v3.1/tool_router/session/${encodeURIComponent(sessionId)}/execute_meta`,{method:"POST",body:JSON.stringify({slug,arguments:args})});}
