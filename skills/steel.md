# Steel browser skill

Steel is a browser adapter, not a truth engine. Create/use a browser session only after the source registry selects a public/authorized source that requires browser interaction. Stay on the intended official domain when possible. Read minimal fields needed for existence/correlation. Never purchase a report, bypass access controls, or turn an inability to reach a record into negative evidence. Persist the execution ID and digest, not an unrestricted browser transcript.
