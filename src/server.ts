import Fastify from "fastify";import cors from "@fastify/cors";import statik from "@fastify/static";import path from "node:path";import { env,assertProductionSafety } from "./config/env.js";import { routes } from "./api/routes.js";import { pool } from "./db/index.js";
assertProductionSafety();const app=Fastify({logger:{level:env.NODE_ENV==="production"?"info":"debug"},bodyLimit:2_000_000,requestTimeout:60_000});await app.register(cors,{origin:false});await app.register(statik,{root:path.resolve("public"),prefix:"/admin/"});await routes(app);app.setErrorHandler((error:any,_req:any,reply:any)=>{app.log.error(error);reply.code(500).send({error:"INTERNAL_ERROR"});});const stop=async()=>{await app.close();await pool.end();process.exit(0);};process.on("SIGTERM",stop);process.on("SIGINT",stop);await app.listen({port:env.PORT,host:"0.0.0.0"});
